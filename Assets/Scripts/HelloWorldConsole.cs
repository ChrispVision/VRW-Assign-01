using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HelloWorldConsole : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //503403 Change console message to Hello Moon
        
        Debug.Log("Hello Moon");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
